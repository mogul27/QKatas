namespace Kata {
    open Microsoft.Quantum.Math;
    
    function ComplexModulus(x : Complex) : Double {
        let (a, b) = x!;
        let modulus = Sqrt(a*a + b*b);

        return modulus;
    }
}
